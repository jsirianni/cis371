package main


// Return byte array of script result
func pythonExec(script string) []byte {
	// Execute script from python script dir

  // Format response as a byte array
  // body := []byte()

  // Get response headers - PASS PATH + SCRIPT FILE
  // headers := okHeaders("/", strconv.Itoa(len(body)))

	return append(headers, body...)
}
